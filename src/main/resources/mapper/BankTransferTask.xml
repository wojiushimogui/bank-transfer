<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="mapper.BankTransferTask">

    <select id="queryById" parameterType="int" resultType="com.wrh.dto.BankTransferTask">
        SELECT * FROM BankTransferTask WHERE ID=#{id}
    </select>

    <!-- update 等方法-->
</mapper>
